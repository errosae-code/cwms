import {createClient} from "@/lib/supabase/server";import {addContribution} from "./actions";
export default async function Contributions(){const s=await createClient();const {data:members}=await s.from("members").select("id,membership_no,full_name").eq("status","active").is("deleted_at",null).order("full_name");const {data:rows}=await s.from("contributions").select("id,amount,contribution_month,contribution_year,reference,members(full_name,membership_no)").is("deleted_at",null).order("created_at",{ascending:false}).limit(30);
return <><h1>Contributions</h1><div className="panel" style={{marginBottom:18}}><form action={addContribution}><div className="formgrid"><div className="field"><label>Member</label><select name="member_id" required><option value="">Select member</option>{members?.map(m=><option key={m.id} value={m.id}>{m.membership_no} — {m.full_name}</option>)}</select></div><div className="field"><label>Amount</label><input name="amount" type="number" step="0.01" defaultValue="3000" required/></div><div className="field"><label>Month</label><select name="contribution_month">{Array.from({length:12},(_,i)=>i+1).map(x=><option key={x}>{x}</option>)}</select></div><div className="field"><label>Year</label><input name="contribution_year" type="number" defaultValue="2026"/></div><div className="field"><label>Payment Date</label><input name="payment_date" type="date" required/></div><div className="field"><label>Reference</label><input name="reference"/></div></div><button className="btn success" style={{marginTop:14}}>Save Contribution</button></form></div>
<div className="panel"><table><thead><tr><th>Member</th><th>Month</th><th>Year</th><th>Amount</th><th>Reference</th></tr></thead><tbody>{rows?.map((r:any)=><tr key={r.id}><td>{r.members?.membership_no} — {r.members?.full_name}</td><td>{r.contribution_month}</td><td>{r.contribution_year}</td><td>KES {Number(r.amount).toLocaleString()}</td><td>{r.reference||"-"}</td></tr>)}</tbody></table></div></>}
