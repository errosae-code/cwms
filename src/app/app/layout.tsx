import Sidebar from "@/components/Sidebar";import {logout} from "./actions";import {createClient} from "@/lib/supabase/server";
export default async function Layout({children}:{children:React.ReactNode}){const s=await createClient();const {data:{user}}=await s.auth.getUser();return <div className="shell"><Sidebar/><main className="main"><div className="topbar"><strong>Chyun Welfare</strong><form action={logout}><span style={{marginRight:12,color:"#6B7280",fontSize:13}}>{user?.email}</span><button className="btn light">Logout</button></form></div><div className="content">{children}</div></main></div>}
