import {createClient} from "@/lib/supabase/server";import {issueLoan} from "./actions";
export default async function Loans(){const s=await createClient();const {data:members}=await s.from("members").select("id,membership_no,full_name").eq("status","active").is("deleted_at",null).order("full_name");const {data:rows}=await s.from("loans").select("*,members(full_name,membership_no)").is("deleted_at",null).order("created_at",{ascending:false});
return <><h1>Loans</h1><div className="panel" style={{marginBottom:18}}><h3>Issue Loan</h3><form action={issueLoan}><div className="formgrid"><div className="field"><label>Member</label><select name="member_id" required><option value="">Select member</option>{members?.map(m=><option key={m.id} value={m.id}>{m.membership_no} — {m.full_name}</option>)}</select></div><div className="field"><label>Principal</label><input name="principal" type="number" step="0.01" required/></div><div className="field"><label>Issue Date</label><input name="issue_date" type="date" required/></div><div className="field"><label>Due Date</label><input name="due_date" type="date" required/></div></div><button className="btn success" style={{marginTop:14}}>Issue Loan</button></form></div>
<div className="panel"><table><thead><tr><th>Loan</th><th>Member</th><th>Principal</th><th>Interest</th><th>Total Outstanding</th><th>Status</th></tr></thead><tbody>{rows?.map((r:any)=><tr key={r.id}><td>{r.loan_no}</td><td>{r.members?.full_name}</td><td>KES {Number(r.outstanding_principal).toLocaleString()}</td><td>KES {Number(r.outstanding_interest).toLocaleString()}</td><td>KES {(Number(r.outstanding_principal)+Number(r.outstanding_interest)).toLocaleString()}</td><td>{r.status}</td></tr>)}</tbody></table></div></>}
